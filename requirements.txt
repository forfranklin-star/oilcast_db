# ---- 核心数据处理 ----
pandas>=2.0,<3.0
numpy>=1.24,<3.0
scipy>=1.10
PyYAML>=6.0
requests>=2.31
feedparser>=6.0.10
beautifulsoup4>=4.12
lxml>=5.0
tqdm>=4.66
xlrd>=2.0.1            # 读取 GPRD 地缘风险指数的 .xls 备份地址（主地址失效时启用）
openpyxl>=3.1

# ---- 建模 ----
scikit-learn>=1.3
statsmodels>=0.14
# Prophet 与 XGBoost 为可选增强（体积较大，默认不安装也可运行全流程）：
# prophet>=1.1.5
# xgboost>=2.0

# ---- 行情数据源（无需 API Key 的真实行情兜底源） ----
yfinance>=0.2.40

# ---- 可视化与交互报告 ----
plotly>=5.18
jinja2>=3.1
streamlit>=1.45

# ---- 测试 ----
pytest>=7.4
